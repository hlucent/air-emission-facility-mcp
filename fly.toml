app = "air-emission-facility-mcp"
primary_region = "nrt"

[build]
dockerfile = "Dockerfile"

[env]
PORT = "8000"

[[services]]
internal_port = 8000
protocol = "http"

[services.concurrency]
type = "connections"
hard_limit = 256
soft_limit = 200
